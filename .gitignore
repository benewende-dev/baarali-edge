# Model weights — never commit these to git
model/*.gguf
model/*.bin
model/*.safetensors

# Prises de la video de soumission — un .mov n a rien a faire dans un depot de code
demo/prise-*.mov

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Local profiler output
submission.json
audit.json


# Poids des candidats au banc d essai
bench/models/
